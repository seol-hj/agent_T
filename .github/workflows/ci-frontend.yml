# ============================================================================
# CI Workflow: frontend
# ============================================================================

name: CI - Frontend

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/frontend/**"
      - ".github/workflows/ci-frontend.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "apps/frontend/**"

env:
  SERVICE_NAME: frontend
  AWS_REGION: ap-northeast-2

jobs:
  determine-env:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    outputs:
      ecr-repo: ${{ steps.env.outputs.ecr-repo }}
    steps:
      - name: Determine environment
        id: env
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "ecr-repo=agent-t-prod/frontend" >> $GITHUB_OUTPUT
          else
            echo "ecr-repo=agent-t-dev/frontend" >> $GITHUB_OUTPUT
          fi

  build-and-push:
    needs: determine-env
    uses: ./.github/workflows/build-and-push.yml
    if: github.event_name == 'push'
    with:
      service-name: frontend
      dockerfile-path: apps/frontend/Dockerfile
      context-path: apps/frontend
      ecr-repository: ${{ needs.determine-env.outputs.ecr-repo }}
      test-command: |
        npm install
        npm run test
        npm run lint
      skip-tests: false
    secrets:
      aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  build-validation:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v5
        with:
          context: apps/frontend
          file: apps/frontend/Dockerfile
          push: false
          tags: ${{ env.SERVICE_NAME }}:${{ github.sha }}
