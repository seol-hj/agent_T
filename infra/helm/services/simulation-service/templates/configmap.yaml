apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "simulation-service.fullname" . }}
  labels:
    {{- include "simulation-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
